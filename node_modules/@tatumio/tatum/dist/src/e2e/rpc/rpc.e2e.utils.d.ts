import { Network } from '../../dto';
export declare const RpcE2eUtils: {
    initConfig: (network: Network, apiKey?: string) => {
        network: Network;
        verbose: boolean;
        retryCount: number;
        retryDelay: number;
        apiKey: {
            v4: string | undefined;
        };
    };
};
