import { Network } from '../../../dto';
import { BaseEvmClass } from '../../../service';
interface evmE2eI {
    network: Network;
    apiKey?: string;
    expected: {
        chainId: number;
    };
    data?: {
        estimateGas?: any;
    };
    skipEstimateGas?: boolean;
}
export declare const EvmE2eUtils: {
    initTatum: (network: Network, apiKey?: string) => Promise<BaseEvmClass>;
    e2e: (evmE2eI: evmE2eI) => void;
};
export {};
