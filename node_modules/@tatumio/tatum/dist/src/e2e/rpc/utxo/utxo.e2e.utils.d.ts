import { Network } from '../../../dto';
import { BaseUtxoClass } from '../../../service';
export declare enum UtxoNetworkType {
    MAIN = "main",
    TEST = "test"
}
interface TatumBtcUtils {
    type: UtxoNetworkType;
    network: Network;
}
export declare const UtxoE2eUtils: {
    initTatum: (network: Network) => Promise<BaseUtxoClass>;
    e2e: ({ type, network }: TatumBtcUtils) => void;
};
export {};
