import { AddressBasedNotification, AddressBasedNotificationDetail, BaseEvmClass, BlockBasedNotification, BlockBasedNotificationDetail, ContractBasedNotification, ContractBasedNotificationDetail, Network } from '../service';
import { ResponseDto } from '../util';
export declare const e2eUtil: {
    subscriptions: {
        getAddress: (network: Network) => string;
        testAddressBasedSubscription: (tatum: BaseEvmClass, address: string, func: (addressBasedNotificationDetail: AddressBasedNotificationDetail) => Promise<ResponseDto<AddressBasedNotification>>) => Promise<string>;
        testContractBasedSubscription: (tatum: BaseEvmClass, contractAddress: string, func: (contractBasedNotificationDetail: ContractBasedNotificationDetail) => Promise<ResponseDto<ContractBasedNotification>>) => Promise<void>;
        testBlockBasedSubscription: (tatum: BaseEvmClass, func: (blockBasedNotificationDetail: BlockBasedNotificationDetail) => Promise<ResponseDto<BlockBasedNotification>>) => Promise<void>;
    };
    isVerbose: boolean;
};
